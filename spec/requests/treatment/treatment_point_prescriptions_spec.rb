require 'spec_helper'

describe "Treatment::PointPrescriptions" do
  describe "GET /treatment_point_prescriptions" do
    it "works! (now write some real specs)" do
      get treatment_point_prescriptions_path
      expect(response.status).to be(200)
    end
  end
end
