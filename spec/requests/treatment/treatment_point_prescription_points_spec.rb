require 'spec_helper'

describe "Treatment::PointPrescriptionPoints" do
  describe "GET /treatment_point_prescription_points" do
    it "works! (now write some real specs)" do
      get treatment_point_prescription_points_path
      expect(response.status).to be(200)
    end
  end
end
