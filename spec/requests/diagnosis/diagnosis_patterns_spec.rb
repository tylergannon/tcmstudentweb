require 'spec_helper'

describe "Diagnosis::Patterns" do
  describe "GET /diagnosis_patterns" do
    it "works! (now write some real specs)" do
      get diagnosis_patterns_path
      expect(response.status).to be(200)
    end
  end
end
