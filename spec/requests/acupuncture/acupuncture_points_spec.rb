require 'spec_helper'

describe "Acupuncture::Points" do
  describe "GET /acupuncture_points" do
    it "works! (now write some real specs)" do
      get acupuncture_points_path
      expect(response.status).to be(200)
    end
  end
end
