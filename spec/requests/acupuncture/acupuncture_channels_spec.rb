require 'spec_helper'

describe "Acupuncture::Channels" do
  describe "GET /acupuncture_channels" do
    it "works! (now write some real specs)" do
      get acupuncture_channels_path
      expect(response.status).to be(200)
    end
  end
end
