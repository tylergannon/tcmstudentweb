require 'spec_helper'

describe "Clinic::Visits" do
  describe "GET /clinic_visits" do
    it "works! (now write some real specs)" do
      get clinic_visits_path
      expect(response.status).to be(200)
    end
  end
end
