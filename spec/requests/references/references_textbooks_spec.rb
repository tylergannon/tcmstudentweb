require 'spec_helper'

describe "References::Textbooks" do
  describe "GET /references_textbooks" do
    it "works! (now write some real specs)" do
      get references_textbooks_path
      expect(response.status).to be(200)
    end
  end
end
