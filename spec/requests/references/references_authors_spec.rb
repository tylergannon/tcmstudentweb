require 'spec_helper'

describe "References::Authors" do
  describe "GET /references_authors" do
    it "works! (now write some real specs)" do
      get references_authors_path
      expect(response.status).to be(200)
    end
  end
end
