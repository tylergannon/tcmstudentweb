require 'spec_helper'

describe "References::Citations" do
  describe "GET /references_citations" do
    it "works! (now write some real specs)" do
      get references_citations_path
      expect(response.status).to be(200)
    end
  end
end
