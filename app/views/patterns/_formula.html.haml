.child
  %p
    = f.label :pinyin, "Formula"
    %br
    != f.text_field_with_auto_complete :pinyin, {}, { :url => formulas_path(:format => :js), :method => :get, :skip_style => true, :with => "'search='+element.value" }

  %p
    = f.hidden_field :_destroy, :class => 'delete'
    = remove_child_link 'remove', f
  %p
    = f.hidden_field :_destroy, :class => 'delete'
    = remove_child_link 'remove', f
