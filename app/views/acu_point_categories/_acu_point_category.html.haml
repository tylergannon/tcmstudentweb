%li[acu_point_category]
  #{acu_point_category.category_name}
  - unless acu_point_category.channels.empty?
    of #{raw link( acu_point_category.channels)} #{acu_point_category.channels.size>1 ? 'channels' : 'channel'}
  - unless acu_point_category.commentary.empty?
    (#{acu_point_category.commentary})
  != edit_link acu_point_category

