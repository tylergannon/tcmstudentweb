.child
  %p
    Pulse:
    = f.text_field_with_auto_complete :pulse_quality_name, {},  { :url => pulse_qualities_path(:format => :js), :method => :get, :skip_style => true, :with => "'search='+element.value" }
    = f.hidden_field :_destroy, :class => 'delete'
    = remove_child_link 'remove', f
