.child
  %p
    Contraindication:
    != f.text_field_with_auto_complete :contraindication_name, {},  { :url => contraindications_path(:format => :js), :method => :get, :skip_style => true, :with => "'search='+element.value" }
    = f.hidden_field :_destroy, :class => 'delete'
    = remove_child_link 'remove', f
