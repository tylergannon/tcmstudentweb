- title "Editing #{@herb.pinyin}"
= form_for @herb, :html => {:multipart => true} do |f|
  = render :partial => 'form', :locals => { :f => f }
  %p
    = f.submit 'Update'
= link_to 'Show', @herb
|
= link_to 'Back', herbs_path
