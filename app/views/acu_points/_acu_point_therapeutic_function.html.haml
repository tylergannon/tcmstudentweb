.child
  %table
    %td
      %tr
        Function:
        !!= f.text_field_with_auto_complete :therapeutic_function_name, {}, { :url => therapeutic_functions_path(:format => :js), :method => :get, :skip_style => true, :with => "'search='+element.value" }
      %tr
        = f.text_area :commentary, :rows => '2', :cols => '40'
        = f.hidden_field :_destroy, :class => 'delete'
        = remove_child_link 'remove', f

