$el = $('#acu_point_info_new');
$el.hide();
$el.remove
$('#acu_point_infos').append('<div id="acu_point_info_#{@acu_point_info.id}"/>');
$el = $('#acu_point_info_#{@acu_point_info.id}');
$el.html(#{raw render_js(:partial=>'acu_point_info', :locals=>{:acu_point_info=>@acu_point_info})});
$el.show('slide');

