%h1 Articles

%h2 Tag Cloud
- tag_cloud(@tags, %w(css1 css2 css3 css4)) do |tag, css_class|
  = link_to tag.name, "/articles/t/#{tag.name}", :class => css_class

@articles.each do |article|
  render article

%br
= link_to 'New article', new_article_path

